"""SmartIngest — agentic document intelligence pipeline.

A multi-agent LangGraph pipeline that classifies, extracts, validates and
routes uploaded documents (invoices, contracts, resumes, IDs).
"""

__version__ = "0.1.0"
