# --- Core orchestration ---
langgraph>=0.2.0
langchain-core>=0.3.0

# --- LLM provider (Gemini multimodal vision + extraction) ---
google-genai>=0.3.0

# --- API layer ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Data modeling & config ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
PyYAML>=6.0

# --- Observability ---
langsmith>=0.1.0

# --- Frontend ---
streamlit>=1.33.0
requests>=2.31.0

# --- Utilities ---
python-dotenv>=1.0.0
pillow>=10.0.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# --- Local package ---
# Install the project itself, not just its deps, so `pip install -r
# requirements.txt` exposes the `smartingest` package. Required on Streamlit
# Community Cloud, whose pip-based deploy runs the UI (which imports
# smartingest.guardrails/models). uv and the Docker image install it anyway.
.
